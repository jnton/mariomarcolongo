ht theme' : 'Switch to dark theme');
     }
+
+    var current = document.documentElement.getAttribute('data-theme') === 'dark' ? 'dark' : 'light';
+    updateThemeControl(current);
+
+    themeBtn.addEventListener('click', function() {
+      var active = document.documentElement.getAttribute('data-theme') === 'dark' ? 'dark' : 'light';
+      var next = active === 'dark' ? 'light' : 'dark';
+      document.documentElement.setAttribute('data-theme', next);
+      updateThemeControl(next);
+      try { localStorage.setItem('theme', next); } catch (e) {}
+    });
   })();
 </script>
diff --git a/src/pages/cv-resume.astro b/src/pages/cv-resume.astro
index 2b3fe2b..06aab11 100644
--- a/src/pages/cv-resume.astro
+++ b/src/pages/cv-resume.astro
@@ -8,7 +8,7 @@ import D from "../../data/source.js";
 
 <Layout
   title="Mario Marcolongo — 2-Page Application Résumé"
-  description="Concise 2-page application résumé of Mario Marcolongo — AI Evaluation & Scientific Research Verification Specialist, Model Behavior Evaluation, and Technical Product Delivery."
+  description={`Concise two-page application résumé of ${D.identity.name} — ${D.identity.jobTitle}.`}
   canonical="https://mariomarcolongo.com/cv-resume.html"
 >
   <Fragment slot="head">
@@ -37,7 +37,7 @@ import D from "../../data/source.js";
         <a href="/cv.html" class="btn">Standard CV (Full)</a>
         <button
           class="btn btn-ai"
-          onclick="copyCvForAI(this)"
+          onclick="copyCanonicalText('/cv-llm.txt', this, '✓ Copied AI CV!')"
           title="Copy complete CV in AI-compatible Markdown format"
           >✨ Copy for AI (Markdown)</button
         >
@@ -53,7 +53,7 @@ import D from "../../data/source.js";
         <div class="cv-header-top">
           <h1 class="cv-name" id="cvName">{D.identity.name}</h1>
           <span class="cv-version-pill print-hide"
-            >2-Page Application Résumé · Line Budget Target: <strong class="budget-target">42 / 44</strong> · July 2026</span
+            >{D.identity.buildVersion} · Updated: {D.identity.evaluationAsOf}</span
           >
         </div>
         <p class="cv-tagline" id="cvTagline">
@@ -193,8 +193,10 @@ import D from "../../data/source.js";
           {D.education.map((edu, idx) => (
             <div class={`edu-item ${idx === D.education.length - 1 ? "resume-edu-last" : "resume-edu"}`}>
               <span class="edu-title">{edu.title}</span>
-              {edu.detail && <> — <span class="edu-detail" set:html={edu.detail} /></>}
-              <span class="edu-period">({edu.period})</span>
+              {edu.period && <> <span class="edu-period">({edu.period})</span></>}
+              {edu.institution && <> — <span class="edu-detail">{edu.institution}</span></>}
+              {edu.status && <> · <span class="edu-status">{edu.status}</span></>}
+              {edu.credentialUrl && <> · <a class="edu-credential" href={edu.credentialUrl} target="_blank" rel="noopener noreferrer">Credential</a></>}
             </div>
           ))}
         </div>
@@ -204,73 +206,21 @@ import D from "../../data/source.js";
 
   <script is:inline>
     function injectPrivateData() {
-      if (typeof window === "undefined") return;
-      const priv = window._privateData || {};
-      const pSlot = document.getElementById("cvPhoneSlot");
-      if (pSlot && priv && priv.phone) {
-        pSlot.innerHTML = `<span>·</span> <a href="tel:${priv.phone.replaceAll(/\s+/g, "")}">${priv.phone}</a>`;
-        pSlot.classList.add("has-phone");
+      const priv = (typeof window !== "undefined" && window._privateData) ? window._privateData : {};
+      const phoneSlot = document.getElementById("cvPhoneSlot");
+      if (priv.phone && phoneSlot) {
+        phoneSlot.innerHTML = `<span>·</span> <a href="tel:${priv.phone.replaceAll(/\s+/g, "")}">${priv.phone}</a>`;
+        phoneSlot.classList.add("has-phone");
       }
     }
 
-    function toggleTheme() {
-      const isDark = document.documentElement.dataset.theme === "dark";
-      const next = isDark ? "light" : "dark";
-      document.documentElement.dataset.theme = next;
-      try {
-        localStorage.setItem("theme", next);
-      } catch (err) {}
-      const btn = document.getElementById("themeBtn");
-      if (btn) btn.textContent = next === "dark" ? "Light Mode" : "Dark Mode";
-    }
-
     function printCV() {
-      window.print();
-    }
-
-    window.addEventListener("DOMContentLoaded", () => {
       injectPrivateData();
-    });
-
-    async function copyCvForAI(btnEl) {
-      const origText = btnEl.textContent;
-      btnEl.textContent = "Copying...";
-      let content = null;
-
-      try {
-        const res = await fetch("/cv-llm.txt");
-        if (res.ok) {
-          content = await res.text();
-        } else {
-          console.error("Failed to fetch /cv-llm.txt:", res.status, res.statusText);
-        }
-      } catch (err) {
-        console.error("Fetch error for CV AI Copy:", err);
-      }
-
-      if (!content) {
-        btnEl.textContent = "Unable to copy";
-        setTimeout(() => (btnEl.textContent = origText), 2000);
-        return;
-      }
-
-      try {
-        await navigator.clipboard.writeText(content);
-        btnEl.textContent = "✓ Copied AI CV!";
-        btnEl.style.background = "var(--emerald, #10B981)";
-        btnEl.style.color = "#fff";
-        setTimeout(() => {
-          btnEl.textContent = origText;
-          btnEl.style.background = "";
-          btnEl.style.color = "";
-        }, 2200);
-      } catch (err) {
-        console.error("Copy error:", err);
-        btnEl.textContent = "Unable to copy";
-        setTimeout(() => (btnEl.textContent = origText), 2000);
-      }
+      window.setTimeout(() => window.print(), 100);
     }
+
+    window.addEventListener("DOMContentLoaded", injectPrivateData);
   </script>
 
-  <SiteFooter version="v2026.07.21" />
+  <SiteFooter version={D.identity.buildVersion} />
 </Layout>
diff --git a/src/pages/cv.astro b/src/pages/cv.astro
index ae8dbbe..2660406 100644
--- a/src/pages/cv.astro
+++ b/src/pages/cv.astro
@@ -4,6 +4,11 @@ import SiteNav from "../components/SiteNav.astro";
 import SiteFooter from "../components/SiteFooter.astro";
 import "../styles/cv.css";
 import D from "../../data/source.js";
+import { buildProfileGraph } from "../lib/structured-data.js";
+
+const pageTitle = "Mario Marcolongo — CV / Curriculum Vitae";
+const pageDescription = `Curriculum Vitae of ${D.identity.name} — ${D.identity.jobTitle}.`;
+const profileJsonLd = buildProfileGraph(D, "https://mariomarcolongo.com/cv.html", pageTitle);
 
 function renderLinksJsx(links) {
   if (!links) return [];
@@ -26,131 +31,13 @@ function renderLinksJsx(links) {
 ---
 
 <Layout
-  title="Mario Marcolongo — CV / Curriculum Vitae"
-  description="Curriculum Vitae of Mario Marcolongo — AI evaluation and scientific research verification specialist, model behavior evaluation, and open science infrastructure."
+  title={pageTitle}
+  description={pageDescription}
   canonical="https://mariomarcolongo.com/cv.html"
 >
   <Fragment slot="head">
     <script is:inline src="/data/source.js"></script>
-    <script type="application/ld+json">
-      {
-        "@context": "https://schema.org",
-        "@graph": [
-          {
-            "@type": "ProfilePage",
-            "@id": "https://mariomarcolongo.com/cv.html#webpage",
-            "name": "Mario Marcolongo — CV / Curriculum Vitae",
-            "url": "https://mariomarcolongo.com/cv.html",
-            "mainEntity": { "@id": "https://mariomarcolongo.com/#person" }
-          },
-          {
-            "@type": "Person",
-            "@id": "https://mariomarcolongo.com/#person",
-            "name": "Mario Marcolongo",
-            "jobTitle": "AI Evaluation & Scientific Research Verification Specialist",
-            "description": "AI evaluation and scientific research verification specialist specializing in model behavior 