class="footer-links" style="align-items: center; gap: 12px;">
+      <div class="footer-links footer-dossier-links">
         <a href="/llms.txt" class="btn btn-sm" target="_blank" rel="noopener noreferrer">llms.txt</a>
         <a href="/llms-full.txt" class="btn btn-sm" target="_blank" rel="noopener noreferrer">llms-full.txt</a>
         <a href="/cv-llm.txt" class="btn btn-sm" target="_blank" rel="noopener noreferrer">cv-llm.txt</a>
-        <button class="btn btn-ai btn-sm" onclick="copyFooterAI(this)" title="Copy complete AI-compatible Markdown Dossier">✨ Copy for AI</button>
+        <button
+          class="btn btn-ai btn-sm"
+          type="button"
+          onclick="copyCanonicalText('/llms-full.txt', this, '✓ Copied Dossier!')"
+          title="Copy the canonical complete AI-compatible dossier"
+        >✨ Copy complete dossier</button>
       </div>
     </div>
   </div>
 </footer>
 
 <script is:inline>
-  async function copyFooterAI(btnEl) {
-    var origText = btnEl.textContent;
-    btnEl.textContent = 'Copying...';
-    var content = null;
-    try {
-      var res = await fetch('/llms-full.txt');
-      if (res.ok) {
-        content = await res.text();
-      } else {
-        console.error('Failed to fetch /llms-full.txt:', res.status, res.statusText);
+  (function exposeCanonicalCopyUtility() {
+    if (window.copyCanonicalText) return;
+
+    window.copyCanonicalText = async function copyCanonicalText(url, btnEl, successText) {
+      var originalText = btnEl.textContent;
+      btnEl.disabled = true;
+      btnEl.textContent = 'Copying...';
+
+      try {
+        var response = await fetch(url, { cache: 'no-store' });
+        if (!response.ok) throw new Error('HTTP ' + response.status);
+        var content = await response.text();
+        if (!content.trim()) throw new Error('Canonical file was empty');
+        await navigator.clipboard.writeText(content);
+        btnEl.textContent = successText || '✓ Copied!';
+        btnEl.classList.add('copy-success');
+      } catch (error) {
+        console.error('Unable to copy canonical file ' + url + ':', error);
+        btnEl.textContent = 'Unable to copy';
       }
-    } catch(err) {
-      console.error('Fetch error during copyFooterAI:', err);
-    }
-    if (!content) {
-      btnEl.textContent = 'Unable to copy';
-      setTimeout(function() { btnEl.textContent = origText; }, 2000);
-      return;
-    }
-    try {
-      await navigator.clipboard.writeText(content);
-      btnEl.textContent = '✓ Copied Dossier!';
-      btnEl.style.background = 'var(--emerald, #10B981)';
-      btnEl.style.color = '#fff';
-      setTimeout(function() {
-        btnEl.textContent = origText;
-        btnEl.style.background = '';
-        btnEl.style.color = '';
+
+      window.setTimeout(function() {
+        btnEl.textContent = originalText;
+        btnEl.disabled = false;
+        btnEl.classList.remove('copy-success');
       }, 2200);
-    } catch (err) {
-      console.error('Clipboard write error:', err);
-      btnEl.textContent = 'Unable to copy';
-      setTimeout(function() { btnEl.textContent = origText; }, 2000);
-    }
-  }
+    };
+  })();
 </script>
diff --git a/src/components/SiteNav.astro b/src/components/SiteNav.astro
index 131deb3..c17ba53 100644
--- a/src/components/SiteNav.astro
+++ b/src/components/SiteNav.astro
@@ -7,80 +7,75 @@ const { activePage = 'home' } = Astro.props;
 ---
 
 <header class="nav print-hide" id="siteNav">
-  <a href="/" class="nav-name" aria-label="Mario Marcolongo — Home">Mario Marcolongo</a>
-  <nav class="nav-actions" aria-label="Primary Navigation">
+  <a href="/" class="nav-name" aria-label="Mario Marcolongo — Home"><span class="nav-name-full">Mario Marcolongo</span><span class="nav-name-short" aria-hidden="true">MM</span></a>
+  <nav class="nav-actions" aria-label="Primary navigation">
     <a
       href="/"
-      class={`nav-link ${activePage === 'home' ? 'active' : ''}`}
+      class={`nav-link nav-overview ${activePage === 'home' ? 'active' : ''}`}
       aria-current={activePage === 'home' ? 'page' : undefined}
     >Overview</a>
     <a
       href="/security.html"
-      class={`nav-link ${activePage === 'security' ? 'active' : ''}`}
+      class={`nav-link nav-evaluation ${activePage === 'security' ? 'active' : ''}`}
       aria-current={activePage === 'security' ? 'page' : undefined}
-    >Model Evaluation</a>
+    ><span class="evaluation-label-full">Evaluation Record</span><span class="evaluation-label-short" aria-hidden="true">Evaluation</span></a>
     <a
       href="/cv.html"
-      class={`nav-link hide-mobile ${activePage === 'cv' ? 'active' : ''}`}
+      class={`nav-link nav-full-cv ${activePage === 'cv' ? 'active' : ''}`}
       aria-current={activePage === 'cv' ? 'page' : undefined}
     >Full CV</a>
     <a
       href="/cv-resume.html"
-      class={`btn btn-primary btn-sm ${activePage === 'resume' ? 'active' : ''}`}
+      class={`btn btn-primary btn-sm nav-resume ${activePage === 'resume' ? 'active' : ''}`}
       aria-current={activePage === 'resume' ? 'page' : undefined}
-    >2-Page Résumé</a>
-    <button class="theme-toggle" id="themeBtn" aria-label="Toggle visual theme" type="button">
+      aria-label="2-Page Application Résumé"
+    ><span class="resume-label-full">2-Page Résumé</span><span class="resume-label-short" aria-hidden="true">Résumé</span></a>
+    <button
+      class="theme-toggle"
+      id="themeBtn"
+      aria-label="Switch to dark theme"
+      aria-pressed="false"
+      type="button"
+    >
       <span class="theme-icon-light" aria-hidden="true">☀️</span>
-      <span class="theme-icon-dark" aria-hidden="true" style="display:none;">🌙</span>
+      <span class="theme-icon-dark" aria-hidden="true" hidden>🌙</span>
     </button>
   </nav>
 </header>
 
 <script is:inline>
   (function initNavAndTheme() {
-    // Scroll handling for nav styling
     var navEl = document.getElementById('siteNav');
     if (navEl) {
       var onScroll = function() {
-        if (window.scrollY > 20) {
-          navEl.classList.add('scrolled');
-        } else {
-          navEl.classList.remove('scrolled');
-        }
+        navEl.classList.toggle('scrolled', window.scrollY > 20);
       };
       window.addEventListener('scroll', onScroll, { passive: true });
       onScroll();
     }
 
-    // Theme toggle button handling
     var themeBtn = document.getElementById('themeBtn');
-    if (themeBtn) {
-      function updateIcons(theme) {
-        var lightIcon = themeBtn.querySelector('.theme-icon-light');
-        var darkIcon = themeBtn.querySelector('.theme-icon-dark');
-        if (lightIcon && darkIcon) {
-          if (theme === 'dark') {
-            lightIcon.style.display = 'none';
-            darkIcon.style.display = 'inline';
-          } else {
-            lightIcon.style.display = 'inline';
-            darkIcon.style.display = 'none';
-          }
-        }
-      }
+    if (!themeBtn) return;
 
-      var current = document.documentElement.getAttribute('data-theme') || 'light';
-      updateIcons(current);
-
-      themeBtn.addEventListener('click', function() {
-        var active = document.documentElement.getAttribute('data-theme') === 'dark' ? 'dark' : 'light';
-        var next = active === 'dark' ? 'light' : 'dark';
-        document.documentElement.setAttribute('data-theme', next);
-        updateIcons(next);
-        try {
-          localStorage.setItem('theme', next);
-        } catch(e) {}
-      });
+    function updateThemeControl(theme) {
+      var isDark = theme === 'dark';
+      var lightIcon = themeBtn.querySelector('.theme-icon-light');
+      var darkIcon = themeBtn.querySelector('.theme-icon-dark');
+      if (lightIcon) lightIcon.hidden = isDark;
+      if (darkIcon) darkIcon.hidden = !isDark;
+      themeBtn.setAttribute('aria-pressed', isDark ? 'true' : 'false');
+      themeBtn.setAttribute('aria-label', isDark ? 'Switch to lig